#include <stdio.h>

void main(void) {
    float a;
    float b;
    int c;
    a = 1;
    b = 1.0;
    c = 1;

    printf("the num a is %f \n", a);
    printf("the num a is %d \n", b);
    printf("the num a is %f \n", c);
}

