#include <stdio.h>

void main(void) {
    unsigned int age;
    double second;
    scanf("%d", &age);
    second = 3.156E10;
    printf("the second is %lf\n", second*age);
    printf("the second is %f\n", second*age);

}

